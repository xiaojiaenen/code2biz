{
  "_comment": "Phase 3 布局微调 hints。键为 Entity.field，值为 transitions 覆盖（键 from->to）。null 表示删除字段。依据：archify 渲染器 anchor 规则（带 channelY/via/labelAt 的边不参与端口摊开，用边侧中点）+ validate 诊断逐条推导。",
  "DeployStep.status": {
    "transitions": {
      "pending->running": { "labelAt": [171, 86] },
      "running->success": { "labelAt": [325, 86] },
      "failed->pending": { "toSide": "bottom" }
    }
  },
  "DeployTask.status": {
    "transitions": {
      "running->failed": { "labelAt": [325, 356] }
    }
  },
  "NodeComponent.install_status": {
    "transitions": {
      "installing->not_installed": { "labelAt": [220, 222] },
      "install_failed->not_installed": { "toSide": "bottom" }
    }
  },
  "NodeComponent.run_status": {
    "transitions": {
      "unknown->stopped": { "route": "top-channel", "channelY": null, "label": null },
      "unknown->error": { "label": null },
      "running->error": { "channelY": 380 },
      "unknown->running": { "labelAt": [192, 205] },
      "error->running": { "toSide": "bottom" }
    }
  }
}
